<p>Экспресс отчет сформирует документ со стандартными параметрами для абонентского отчета за период с {{"first day of this month"|date('d.m.Y')}} по {{"last day of this month"|date('d.m.Y')}}</p>
<script>
        require(['jquery'], function($){
            $('#express').click(function(e){
                if($(this).attr('href') == '#'){
                    //e.preventDefault();
                    var id = {{report_id}};
                    var client = $('[name="form\[client\]"]').val();
                    $(this).attr('href', '' + id + '/default/' + client);
                }
                
            });
        });
        </script>