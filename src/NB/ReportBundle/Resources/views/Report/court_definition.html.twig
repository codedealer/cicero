<p>Экспресс отчет сформирует документ со стандартными параметрами для судебного дела</p>
<script>
        require(['jquery'], function($){
            $('#express').click(function(e){
                if($(this).attr('href') == '#'){
                    //e.preventDefault();
                    var id = {{report_id}};
                    var client = $('[name="form\[court\]"]').val();
                    $(this).attr('href', '' + id + '/default/' + client);
                }
                
            });
        });
        </script>